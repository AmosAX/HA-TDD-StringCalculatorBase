
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.mockito.Mockito.*;

public class LoggerTest {

/*
    @Test
    public void testMockLogger(){
        Logger mockLogger = mock(Logger.class);
        // Skapa en instans av MyComponent och injicera mock-loggern
        StringCalculatorTest stringcalc = new StringCalculator(mockLogger);
        // Anropa metoden som du vill testa
        stringcalc.add();


        // Verifiera att loggern anropas med rätt metod och eventuella argument
        verify(mockLogger).log("Doing something...");

    }

 */
}
